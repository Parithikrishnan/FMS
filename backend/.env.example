DB_HOST=postgres
DB_PORT=5432
DB_NAME=fms
DB_USER=fms
DB_PASSWORD=fms123

# Optional
# DATABASE_URL=sqlite:///fms.db        # overrides DB_* (local runs without Postgres)
# APP_TIMEZONE=Asia/Kolkata            # defines "today" for the dashboard
# DEFAULT_CASE_ASSIGNEE=Fraud Analyst
# SEED_DEMO_DATA=true
# CORS_ORIGINS=http://localhost:5173   # only needed if the frontend is not behind nginx
